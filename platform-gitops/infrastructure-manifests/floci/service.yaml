apiVersion: v1
kind: Service
metadata:
  name: floci
  namespace: platform-infra
  labels:
    app: floci
spec:
  type: ClusterIP
  ports:
    - port: 4566
      targetPort: 4566
      name: edge
  selector:
    app: floci
