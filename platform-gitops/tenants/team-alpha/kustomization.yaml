apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tenant-team-alpha

resources:
  - ../base
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  team: alpha
