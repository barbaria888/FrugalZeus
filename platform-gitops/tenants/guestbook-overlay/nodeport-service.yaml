apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui-nodeport
  namespace: tenant-guestbook
  labels:
    app: guestbook-ui
spec:
  type: NodePort
  selector:
    app: guestbook-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30800
